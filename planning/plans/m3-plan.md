# Plan: M3 · From Data to Model — From Raw Data to a Trained Model

**Goal:** Author From Raw Data to a Trained Model fully (lesson + lab + quiz) and validate the lab live.

**Learning objectives:**
- Clean a raw dataset and explain what was dropped and why
- Build engineered features and save the transformation as a reusable preprocessor
- Split data into training and test sets and read X_train, y_train, X_test, y_test correctly
- Define algorithm and hyperparameter grids and compare the resulting runs
- Track every experiment in MLflow and defend which model won

**Lab intent:** Run the processing, feature engineering and training scripts, producing a trained model and a preprocessor, with every run logged to MLflow.

## Tasks
- [ ] Lesson — analogy-first concepts + ≥1 Mermaid diagram
- [ ] Lab — copy-runnable steps + Expected output + Teardown
- [ ] Validate lab live → evidence in `planning/lab-tests/m3.md`
- [ ] Quiz — 4–6 questions, ≥1 multiSelect, every option explained
- [ ] Build green; ROADMAP + STATE updated
