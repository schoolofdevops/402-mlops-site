---
sidebar_position: 4
title: 'Deep Dive: Deploying the Model on Kubernetes'
sidebar_label: 'Deep Dive (Part 2)'
---

# Deep Dive — Deploying the Model on Kubernetes

:::note[Scaffolded — not yet authored]

This is a scaffold stub for the **M6 · Deploying on Kubernetes** Deep Dive (Part 2). `course-authoring`
(via the `deep-dive-author` skill) writes the advanced "under the hood" material here — the
payload that goes BEYOND the core lab — then `lab-validation` folds real Expected output in and
runs `labs/m6/deep-dive.checks.json` as a separate stage.

:::

## What goes here

How a Service actually routes to Pods through endpoints and kube-proxy, and what breaks when label selectors do not match.
