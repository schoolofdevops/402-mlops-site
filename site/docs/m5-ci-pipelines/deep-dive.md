---
sidebar_position: 4
title: 'Deep Dive: Building an MLOps CI Pipeline with GitHub Actions'
sidebar_label: 'Deep Dive (Part 2)'
---

# Deep Dive — Building an MLOps CI Pipeline with GitHub Actions

:::note[Scaffolded — not yet authored]

This is a scaffold stub for the **M5 · CI Pipelines with GitHub Actions** Deep Dive (Part 2). `course-authoring`
(via the `deep-dive-author` skill) writes the advanced "under the hood" material here — the
payload that goes BEYOND the core lab — then `lab-validation` folds real Expected output in and
runs `labs/m5/deep-dive.checks.json` as a separate stage.

:::

## What goes here

Why a mutable latest tag breaks every downstream system, and how commit-SHA tagging changes the pipeline and the manifests together.
