---
sidebar_position: 4
title: 'Deep Dive: Setting up your MLOps Workstation'
sidebar_label: 'Deep Dive (Part 2)'
---

# Deep Dive — Setting up your MLOps Workstation

:::note[Scaffolded — not yet authored]

This is a scaffold stub for the **M2 · Environment Setup** Deep Dive (Part 2). `course-authoring`
(via the `deep-dive-author` skill) writes the advanced "under the hood" material here — the
payload that goes BEYOND the core lab — then `lab-validation` folds real Expected output in and
runs `labs/m2/deep-dive.checks.json` as a separate stage.

:::

## What goes here

Why uv replaces pip and venv, how MLflow stores runs behind the tracking server, and what to change when moving from a local tracking server to a shared one.
