---
sidebar_position: 4
title: 'Deep Dive: From Raw Data to a Trained Model'
sidebar_label: 'Deep Dive (Part 2)'
---

# Deep Dive — From Raw Data to a Trained Model

:::note[Scaffolded — not yet authored]

This is a scaffold stub for the **M3 · From Data to Model** Deep Dive (Part 2). `course-authoring`
(via the `deep-dive-author` skill) writes the advanced "under the hood" material here — the
payload that goes BEYOND the core lab — then `lab-validation` folds real Expected output in and
runs `labs/m3/deep-dive.checks.json` as a separate stage.

:::

## What goes here

Training and inference feature parity — why the preprocessor must be saved rather than reconstructed, and how the two silently drift apart.
